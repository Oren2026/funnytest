# ============================================================
# Evolution Compiler — Warehouse Management System
# ============================================================
#
# Intent: 倉儲管理系統
# Features: 新增 / 編輯 / 刪除、搜尋、篩選、排序
#            狀態標示、刪除確認、操作提示、低庫存警示
#
# Targets: html | react
#
# Usage:
#   python3 intent_parser.py
#   python3 engine.py warehouse.seed
# ============================================================

<intent>
倉儲管理系統
上方導航列含系統標題，右側有新增按鈕
主要區域為庫存列表表格，顯示：名稱、分類、數量、狀態、最後更新
狀態分為三級：正常（綠）、庫存不足（黃）、缺貨（紅）
支援關鍵字搜尋、分類篩選、欄位排序
支援新增、編輯、刪除操作
刪除前彈出確認對話框
所有操作完成後顯示 toast 提示訊息
</intent>

<env target="html" />
<style theme="minimal-dark" />
