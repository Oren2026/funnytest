# skill: table-data

## Contract
- **語義承諾**：渲染一個可排序的資料表格，顯示庫存明細（名稱、分類、數量、狀態、最後更新、操作）
- **輸入資料格式**：
  ```json
  {
    "items": Array<{
      "id": number,
      "name": string,
      "category": string,
      "quantity": number,
      "minStock": number,
      "updatedAt": string
    }>,
    "onEdit": function,
    "onDelete": function
  }
  ```
- **輸出語義**：HTML `<table>` 元素，帶有 thead/tbody slots，支援排序與空狀態顯示
- **操作邊界**：
  - ✅ 做：根據 items 動態渲染資料列、顯示庫存狀態 badge、支援排序切換、顯示空狀態
  - ❌ 不做：新增/刪除/編輯背後的資料邏輯、不處理 API 呼叫、不操作 localStorage
- **失敗信號**：
  - `items` 不是陣列 → 渲染 empty-state slot
  - `items` 长度为 0 → 顯示「尚無庫存資料」

## Dependencies
- **依賴**：`badge-status`（庫存狀態顯示）
- **可選依賴**：`button-primary`（編輯按鈕）、`button-danger`（刪除按鈕）、`modal-form`（表單弹窗）、`search-bar`（搜尋過濾）
- **排斥**：無

## Slots
- **slot:thead**：欄位標題列，靜態定義（名稱、分類、數量、狀態、最後更新、操作）
- **slot:tbody**：動態資料行，由 Composer 注入 `_renderRows(items, onEdit, onDelete)`
- **slot:empty-state**：當 items 為空時顯示的提示訊息，預設「尚無庫存資料」
- **slot:filter**：搜尋/過濾控制區，由 search-bar skill 注入

## Boundaries
- **系統邊界**：Presentation Layer（只處理 UI 渲染，不管資料邏輯）
- **操作邊界**：
  - ✅ 操作：DOM 渲染、使用者互動（點擊排序、切換分頁）、根據 quantity 計算庫存狀態
  - ❌ 不操作：localStorage、網路請求、函式級變數
- **狀態邊界**：Stateless（不保有內部狀態，視覺狀態由 UI 操作類處理）

## Examples

### 基本用法
**輸入**：
```json
{
  "items": [
    {
      "id": 1,
      "name": "螺絲",
      "category": "工具",
      "quantity": 100,
      "minStock": 10,
      "updatedAt": "2024-01-15T08:30:00Z"
    }
  ],
  "onEdit": "() => {}",
  "onDelete": "() => {}"
}
```
**輸出**：
```html
<div class="inventory-table-wrapper">
  <table class="inventory-table">
    <thead>
      <tr>
        <th>名稱</th>
        <th>分類</th>
        <th>數量</th>
        <th>狀態</th>
        <th>最後更新</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>螺絲</td>
        <td><span class="category-tag cat-tools">工具</span></td>
        <td>100</td>
        <td><span class="badge badge-ok">正常</span></td>
        <td>2024/1/15</td>
        <td>
          <div class="action-btns">
            <button class="btn-edit">編輯</button>
            <button class="btn-delete">刪除</button>
          </div>
        </td>
      </tr>
    </tbody>
  </table>
</div>
```

### 空資料
**輸入**：`{ "items": [], "onEdit": ..., "onDelete": ... }`
**輸出**：
```html
<tr><td colSpan="6" class="empty-state">尚無庫存資料</td></tr>
```

### 庫存不足
**輸入**：`{ "items": [{ "quantity": 5, "minStock": 10, ... }], ... }`
**輸出**：
```html
<td><span class="badge badge-warning">庫存不足</span></td>
```

### 缺貨
**輸入**：`{ "items": [{ "quantity": 0, ... }], ... }`
**輸出**：
```html
<td><span class="badge badge-danger">缺貨</span></td>
```
